<h1>
  <%= @article.title %>
    <small>
      <a href=<%= edit_article_path(@article) %> ><i class="fa fa-pencil"></i></a>

      <a data-confirm="Are you sure?" rel="nofollow" data-method="delete" href=<%= article_path(@article) %> >
        <i class="fa fa-trash-o"></i>
      </a>
    </small>
</h1>

<hr>
<p><%= @article.content %></p>
